using System;
using System.Threading.Tasks;



namespace CsLearning
{

    public class SpanTLesson001 : ILesson
    {
        public async Task Run()
        {
            // позволяет создать коллекцию данных, хранимую в стеке, но доступ к которой осуществляется по ссылке

            //var span = new Span<int>(new[]{1,2,3});

            

            // является расширением типа Span<T> и используется для потокобезопасного доступа по ссылке к коллекции хранимой в стеке

            //var memory = new Memory<int>();
        }







    }

}
